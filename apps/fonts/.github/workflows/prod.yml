# name: Font production pipeline
# on:
#   push:
#     branches:
#       - main
# jobs:
#   deploy:
#     name: Production publising
#     runs-on: self-hosted
#     container:
#       image: ubuntu:latest
#     steps:
#       - name: CHECKOUT project
#         uses: actions/checkout@v3
#         with:
#           repository: aziontech/fonts
#           ref: main
#           path: '.'
#           clean: true
#           fetch-depth: '0'
#           lfs: 'false'
#       - name: PREPARE TO DEPLOYMENT
#         run: |
#           rm -rf .github .gitignore cover.png LICENSE README.md
#       - name: DEPLOYMENT
#         uses: patrickwyler/gcs-bucket-sync-action@1.3
#         with:
#           secrets: ${{ secrets.GCP_EXP_ENG }}
#           bucket: azfonts
#       - name: CONFIGURING gcloud CLI
#         uses: 'google-github-actions/auth@v2'
#         with:
#           credentials_json: ${{ secrets.GCP_EXP_ENG }}
#       - uses: 'google-github-actions/setup-gcloud@v2'
#         with:
#           version: '>= 363.0.0'
#       - name: UPDATING cache-control header from Google Cloud Storage
#         run: |
#           gcloud storage objects update -r gs://azfonts --cache-control="public, max-age=120"
