# Archive — shell components kept for reference, not mounted

Nothing in this folder is imported. These are implementations the team decided
against **while they were working**, kept whole because the reason they were
replaced was a design direction rather than a defect: reading the real file
answers "how did the previous one behave?" in a way a diff in the history does
not, and it is what a rebuild starts from if the direction reverses.

Relative imports have been re-pointed one level up so the files still resolve if
one is mounted again; they are otherwise untouched.

| File                         | Was                                                                                                                                                                                                                                                                                                      | Replaced by                                                                                                     | Why                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `AccountSwitcherPopover.vue` | The tenancy chain as THREE pills — organization / account / workspace — each its own popover, joined by `/` separators. One component with a `kind` prop, mounted three times.                                                                                                                           | [`../TenancySwitcher.vue`](../TenancySwitcher.vue)                                                              | The header is unified: one switch component for the whole chain, with the organization and the workspace side by side in a single panel. The three-pill chain worked, but it spent the header's whole width on identity and asked the reader to know which pill held which level.                                                                                                    |
| `TenancySwitcher.vue`        | The whole tenancy chain in ONE control: a pill naming organization / workspace, opening a two-column panel (organizations beside workspaces, each searchable past five rows and ordered by recents), with the account as a band across its top and a plan-gated "New organization" footer. ⌘O opened it. | [`../AccountSwitcher.vue`](../AccountSwitcher.vue) + [`../SwitchAccountDialog.vue`](../SwitchAccountDialog.vue) | The organization and workspace tiers are parked while the product settles its tenancy words (brand/reseller/client → Organization/Group/Workspace). What is left is the tier an operator acts as, so the header names the ACCOUNT and one click opens its tree. The linked org → workspace panel is a direction to come back to, not a defect — which is why the file is kept whole. |
