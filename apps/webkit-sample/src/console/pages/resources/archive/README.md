# Archive — Creation Center implementations kept for reference, not mounted

Nothing in this folder is imported. These are implementations the team decided
against **while they were working**, kept whole because the reason they were
replaced was a design direction rather than a defect: reading the real file
answers "how did the previous one behave?" in a way a diff in the history does
not, and it is what a rebuild starts from if the direction reverses.

Relative imports have been re-pointed one level up so the files still resolve if
one is mounted again; they are otherwise untouched. `CREATION_CENTER_PATH` and
`CREATION_CENTER_LABEL` are still exported from
[`../../../lib/behavior/create-origin.js`](../../../lib/behavior/create-origin.js)
for the same reason — a remounted rail sends `?from=/create` and the create pages
still read it.

| File                 | Was                                                                                                                                                                                                                                                                                                                                                                                                                                             | Replaced by                                      | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CreationCenter.vue` | The Center as a RAIL over ONE PANE: a `Menu` on the left listing the two ways to deploy code (`Import from GitHub`, `Templates`) and, under a `Resources` heading, every first-level object an account can create — fourteen rows. A view row swapped the pane (`?method=`, with `KeepAlive`); a resource row opened that object's create page at `/<module>/new` carrying `?from=/create`, so the flow's crumb, back and Cancel returned here. | [`../CreationCenter.vue`](../CreationCenter.vue) | The Center is the screen for a reader who has code to deploy, and the two ways to deploy it are the whole screen — so showing them one at a time spent a rail, a pane swap and a URL parameter to hide the other half of the answer. The fourteen resource rows were the rail's reason to exist, and they were the part that did not belong: creating a certificate or a bucket is a task a reader arrives at from the module they are already working in, not a way to start a deploy. Both halves are back side by side, each owning itself (`../creation/`). |
| `create-menu.js`     | Every first-level resource as one ordered list — the registry the rail's `Resources` section laid out, ten rows read from the resource spec ([`../../../lib/data/create-resources.js`](../../../lib/data/create-resources.js)) and four hand-written flows declared in the file.                                                                                                                                                                | —                                                | It fed exactly one surface, and that surface is the archived rail. The header's global Create had already stopped using it (it makes one promise — open the Center — see [`../../../components/shell/AppLayout.vue`](../../../components/shell/AppLayout.vue)), so with the rail gone it had no consumer left. It travels with the file it was written for: a rebuild of the rail starts from both.                                                                                                                                                             |
