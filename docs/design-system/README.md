# Design System — Workspace

Este diretório é gerado pelos comandos `/design-*` do Claude Code.

- `tokens/` — documentação dos tokens de design
- `components/` — design specs por componente
- `.cache/` — arquivos temporários (não commitados)
